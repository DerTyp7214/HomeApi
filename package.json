{
  "name": "home-api",
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:api": "pip3 install -r api/requirements.txt && cd api && alembic upgrade head && cd ..",
    "dev:api": "cd api && uvicorn app:app --reload --host 0.0.0.0",
    "start:api": "python api/main.py",
    "install:web": "cd web && pnpm install && cd ..",
    "dev:web": "cd web && pnpm run dev && cd ..",
    "build:web": "cd web && pnpm run build && cd ..",
    "preview:web": "cd web && pnpm run preview && cd ..",
    "check:web": "cd web && pnpm run check && cd ..",
    "deploy:web": "ts-node-esm scripts/deploy.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/noble": "^0.0.41",
    "@types/node": "^20.1.4",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "chalk": "^5.2.0",
    "fs-extra": "^11.1.1",
    "log-update": "^5.0.1"
  }
}